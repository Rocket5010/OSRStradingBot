# Conventions

- **Code and GUI:** English by default (unless specified otherwise).
- **Chat:** Norwegian.
- **Vault docs:** English — keeps wikilinks aligned with code identifiers (module names, [[Position Lifecycle|states]], strategy names).
- **Theme:** dark + gold, driven by CSS variables (`--gold`, `--bg`) so restyling never touches backend. See [[Architecture Overview]].
